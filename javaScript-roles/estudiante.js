// No hay eventos adicionales.
console.log("Panel del estudiante cargado.");

 // Redirigir al login
 function logout() {
    window.location.href = "../index.html"; // Cambia la ruta si el login tiene otro nombre
  }
